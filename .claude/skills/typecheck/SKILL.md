---
description: Run TypeScript compiler and React Router route type generation
disable-model-invocation: false
---

Run the TypeScript typecheck script to ensure type safety across all React Router route definitions, components, and utility libraries:

```bash
npm run typecheck
```

Review any compiler errors and provide fixes maintaining strict TypeScript standards.
